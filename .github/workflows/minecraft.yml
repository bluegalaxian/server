name: PaperMC Server (No Plugins) + Cloudflare Tunnel

on:
  workflow_dispatch:

jobs:
  server:
    runs-on: ubuntu-latest

    steps:
      - name: Install Java + tools
        run: |
          sudo apt update
          sudo apt install -y openjdk-17-jre wget curl

      - name: Setup Minecraft Server
        run: |
          mkdir mc
          cd mc

          # Download PaperMC 1.20.4
          wget -O server.jar https://api.papermc.io/v2/projects/paper/versions/1.20.4/builds/400/downloads/paper-1.20.4-400.jar

          # Accept EULA
          echo "eula=true" > eula.txt

          # Basic server config tweak (optional but helpful)
          echo "online-mode=true" >> server.properties

      - name: Install Cloudflared
        run: |
          curl -L https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 -o cloudflared
          chmod +x cloudflared
          sudo mv cloudflared /usr/local/bin/

      - name: Start Server + Tunnel
        run: |
          cd mc

          # Start Minecraft server
          java -Xmx2G -Xms1G -jar server.jar nogui &

          sleep 20

          # Start Cloudflare Tunnel (YOUR TOKEN REQUIRED)
          cloudflared tunnel --no-autoupdate run --token ${{ secrets.CLOUDFLARE_TUNNEL_TOKEN }}
